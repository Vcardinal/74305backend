<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Vista del Carrito</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(to right, #fce3ec, #f9f0ff);
            font-family: 'Segoe UI', sans-serif;
            padding: 2rem;
        }
        h1 {
            color: #b86b94;
        }
        .carrito-lista li {
            background: #fff;
            border: 1px solid #e6cce6;
            border-radius: 10px;
            padding: 0.5rem 1rem;
            margin-bottom: 0.5rem;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1 class="mb-4">🛒 Productos en tu carrito</h1>

        {{#if carrito.length}}
            <ul id="carrito-productos" class="carrito-lista list-unstyled">
                {{#each carrito}}
                    <li><strong>{{nombre}}</strong> x {{quantity}} = ${{multiply precio quantity}}</li>
                {{/each}}
            </ul>
            <h5 class="mt-4">Total: ${{total}}</h5>
        {{else}}
            <p>Tu carrito está vacío.</p>
        {{/if}}
    </div>
</body>
</html>


