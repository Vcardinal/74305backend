<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Carrito Delicakes</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(to right, #ffe4ec, #e6f6ff);
      padding: 2rem;
      font-family: 'Segoe UI', sans-serif;
    }
    h1 {
      color: #c75287;
      text-align: center;
      margin-bottom: 2rem;
    }
    .total {
      font-weight: bold;
      font-size: 1.2rem;
      text-align: right;
      color: #5c3566;
    }
  </style>
</head>
<body>

  <h1>🛒 Resumen del carrito</h1>

  <div class="container">
    {{#if carrito.length}}
      <ul class="list-group mb-4">
        {{#each carrito}}
          <li class="list-group-item d-flex justify-content-between align-items-center">
            <span>{{nombre}} (x{{quantity}})</span>
            <span>${{precio}}</span>
          </li>
        {{/each}}
      </ul>
      <p class="total">Total: ${{total}}</p>
    {{else}}
      <p class="text-center">No hay productos en el carrito.</p>
    {{/if}}
  </div>

</body>
</html>
