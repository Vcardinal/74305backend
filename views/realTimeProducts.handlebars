<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Productos en tiempo real</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #fdeef7, #e3f6ff);
            font-family: 'Segoe UI', sans-serif;
            padding: 2rem;
        }
        h1 {
            color: #b86b94;
            font-weight: bold;
        }
        .form-control, .btn {
            border-radius: 1rem;
        }
        .product-item {
            background-color: #fff6fa;
            border: 1px solid #f3cfe5;
            border-radius: 10px;
            padding: 0.5rem 1rem;
            margin-bottom: 0.5rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        .btn-agregar {
            background-color: #f497c4;
            color: white;
        }
        .btn-agregar:hover {
            background-color: #e07aad;
        }
        .btn-eliminar {
            background-color: #ff5f7e;
        }
        .btn-eliminar:hover {
            background-color: #d93c5c;
        }
        .cart-list-item {
            background-color: #eafff1;
            border: 1px solid #bde9d1;
            border-radius: 10px;
            padding: 0.5rem 1rem;
            margin-bottom: 0.5rem;
        }
    </style>
</head>
<body>

<div class="container">
    <h1 class="text-center mb-4">
        <img src="https://i.pinimg.com/736x/ad/01/ce/ad01ce6b0e351fb94b53a1592321df42.jpg" width="40" style="border-radius: 50%;">
        Productos en tiempo real
    </h1>

    <!-- Formulario para agregar producto -->
    <form id="productForm" class="row g-3 justify-content-center mb-4">
        <div class="col-md-4">
            <input type="text" name="nombre" placeholder="Nombre del producto" class="form-control" required>
        </div>
        <div class="col-md-3">
            <input type="number" name="precio" placeholder="Precio" class="form-control" required>
        </div>
        <div class="col-md-2">
            <button type="submit" class="btn btn-agregar w-100">Agregar</button>
        </div>
    </form>

    <!-- Productos disponibles -->
    <h4>🧁 Productos disponibles</h4>
    <ul id="productList" class="list-unstyled">
        {{#each products}}
        <li class="product-item">
            <span><strong>{{nombre}}</strong> – ${{precio}}</span>
            <div>
                <button class="btn btn-sm btn-success me-2" onclick="agregarAlCarrito({{id}})">➕</button>
                <button class="btn btn-sm btn-danger btn-eliminar" onclick="eliminarProducto({{id}})">🗑️</button>
            </div>
        </li>
        {{/each}}
    </ul>

    <!-- Carrito -->
    <h4 class="mt-5">🛒 Productos agregados al carrito</h4>
    <ul id="cartList" class="list-unstyled" id="carrito-productos">
        {{#each carrito}}
        <li class="cart-list-item">
            {{nombre}} x {{quantity}} = ${{multiply precio quantity}}
        </li>
        {{/each}}
    </ul>
</div>

<script src="/socket.io/socket.io.js"></script>
<script src="/realTime.js"></script>

</body>
</html>










